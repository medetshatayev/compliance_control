version: "3.8"

services:
  compliance:
    build: .
    container_name: compliance_service
    env_file: .env
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1
