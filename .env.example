# LightRAG Server Configuration
LIGHTRAG_BASE_URL=http://localhost:8000
# Choose one of the authentication methods below

# 1. API Key Authentication (if your server uses X-API-Key header)
LIGHTRAG_API_KEY=

# 2. OAuth2 Password Flow Authentication (if your server uses username/password at /login)
LIGHTRAG_USERNAME=
LIGHTRAG_PASSWORD=

# Granular timeouts for the HTTP client connecting to LightRAG
LIGHTRAG_CONNECT_TIMEOUT=10
LIGHTRAG_READ_TIMEOUT=180
LIGHTRAG_WRITE_TIMEOUT=180
LIGHTRAG_POOL_TIMEOUT=60

# Optional: Number of retries on transient network errors
LIGHTRAG_RETRIES=1
# Backoff factor for retries (e.g. 5 -> sleep 5s)
LIGHTRAG_RETRY_BACKOFF=5

# -- FastAPI Server (Uvicorn) Configuration --
# Number of worker processes for Uvicorn
UVICORN_WORKERS=1
# Keep-alive timeout in seconds for client connections
UVICORN_TIMEOUT_KEEP_ALIVE=120
